# Python 3.10+ توصیه می‌شود

# Async + HTTP
aiohttp>=3.9
websockets>=11

# Data / ML
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
joblib>=1.3

# NLP (احساسات، با fallback روی VADER)
vaderSentiment>=3.3.2
# اختیاری: مدل‌های ترنسفورمر (در صورت نصب، به‌جای VADER استفاده می‌شود)
# transformers>=4.40
# torch>=2.1

# صرافی‌ها (Async ccxt)
ccxt>=4.0

# پایگاه داده (SQLAlchemy + درایور اختیاری Postgres)
SQLAlchemy>=2.0
# اگر از Postgres استفاده می‌کنی یکی از این درایورها را اضافه کن:
# psycopg[binary]>=3.1    # (پیشنهادی، psycopg3)
# یا
# psycopg2-binary>=2.9

# تلگرام (نسخه 20+ با ApplicationBuilder)
python-telegram-bot>=20.3,<21